
// disc folder contain all js files that is conversion of ts files
function greet(name: string): string { // second string indicates return type
    return `Hello, ${name}!`;
}
console.log(greet("mayank"));